
def verificar_bissexto(ano):
    if (ano % 4 == 0 and ano % 100 != 0) or ano % 400 == 0:
        return "SIM"
    else:
        return "NÃO"

# Entrada do usuário
ano = int(input())

# Exibindo a saída
print(verificar_bissexto(ano))
